import Header from '@widgets/header/header';
import MenuForm from '@widgets/menuForm';

const Menu = () => {
  return (
    <>
      <Header />
      <MenuForm />
    </>
  );
};

export default Menu;
